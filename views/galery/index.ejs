<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" languague="javascript" src="./errorHandlerHtml.js"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/images.css">
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/error.css">
    <link rel="stylesheet" href="/css/banner.css">
    <link rel="stylesheet" href="/css/header.css">
    <title>Spider - Galery</title>
    <style>
        body {
            text-align: center;
            background-color: #212529;
        }

        figcaption {
            text-align: center;
        }

        .material-symbols-outlined {
            float: right;
            background-color: rgba(33, 8, 52, 80);
            border: none;
            color: white;
            position: absolute; top:0; left:100; margin:140px 0px 0px 130px;
            margin-top: -0rem;
            margin-left: 6.9rem;
        }

        main {
            height: fit-content;
        }

        p {
            color: #FFF;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                
            </ul>
        </nav>
        <div id="banner">
            <figure>
                <img src="<%= banner %>" alt="Banner">
            </figure>
        </div>
        <nav>
            <ul>
                <li><a href="/home">About Me</a></li>
                <li><a href="/galery">Galery</a></li>
                <li><a href="/price-list">Price list</a></li>
                <li><a href="/payment">Payment / Patreon</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <div>
            <p><%= lastSeen %></p>
            <h1>GALERY</h1>
            <div id="error-msg" class="hidden">
                <p></p>
            </div>
            <div id="gallery">
                <% for (var i=0; i<images.length; i++) {%> 
                        <div class="item">
                            <figure>
                                <a href="/galery/<%= images[i].id %>">
                                    <img src="<%= images[i].url %>" alt="<%= images[i].caption %>">
                                </a>
                                <% if(isSpider) { %>
                                    <form action="/delete-image?id=<%= images[i].id %>" method="post" id="delete-form-<%= images[i].id %>">
                                        <button type="submit" class="material-symbols-outlined">delete</button>
                                    </form>
                                <% } %>
                                <figcaption>
                                    <%= images[i].caption %> 
                                </figcaption>
                            </figure>
                        </div id="item">
                <% }%>  
            </div>
        </div>
    </main>
    <footer>
        <div>
            <p>&copy Pr1v4cy</p>
        </div>
        <div>
            <ul>
                <li><p>OoSpideroO#5203</p></li>
            </ul>
        </div>
    </footer>
     
</body>
</html>