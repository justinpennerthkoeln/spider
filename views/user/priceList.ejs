<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="text/javascript" languague="javascript" src="./errorHandlerHtml.js"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link rel="stylesheet" href="/css/navigation.css">
    <link rel="stylesheet" href="/css/error.css">
    <link rel="stylesheet" href="/css/form.css">
    <link rel="stylesheet" href="/css/banner.css">
    <link rel="stylesheet" href="/css/header.css">
    <title>Spider - Price List</title>
    <style> 
        body {
            text-align: center;
            background-color: #212529;
        }

        .example {
            display: flex;
            flex-direction: row;
            justify-content: center;
            align-items: center;
            color: #FFF;
            border: #FFF 1px solid;
            background-color: rgba(33, 8, 52, 80);
            margin: 1rem auto;
            max-width: 40rem;
            margin-bottom: 5rem;
        }

        .example img {
            width: 50%;
            height: 50%;
        }

        .example p {
            margin: 0;
            padding: 0;
        }

        .hasButton {
            margin-left: 8rem;
        }
        
        .example .text {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            width: 50%;
        }

        .example span {
            width: 0.5rem;
            margin-right: 8rem;
        }

        .example span button {
            background-color: rgba(255,0,0,0);
            padding-bottom: 0;
        }

        #prices {
            display: flex;
            flex-direction: row;
            justify-content: center;
            align-items: center;
            flex-wrap: wrap;
        }

        form button {
            float: left;
        }

        p {
            color: #FFF;
        }


    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                
            </ul>
        </nav>
        <div id="banner">
            <figure>
                <img src="<%= banner %>" alt="Banner">
            </figure>
        </div>
        <nav>
            <ul>
                <li><a href="/home">About Me</a></li>
                <li><a href="/galery">Galery</a></li>
                <li><a href="/price-list">Price list</a></li>
                <li><a href="/payment">Payment / Patreon</a></li>
            </ul>
        </nav>
    </header>
    <p><%= lastSeen %></p>
    <h1>PRICE LIST</h1>

    <div id="prices">
        <% for (var i=0; i<prices.length; i++) { %>
            
            <div class="example">
                <img src="<%= prices[i].url %>" alt="alt">
                <div class="text">
                        <p><%= prices[i].tag1 %></p>
                        <p><%= prices[i].tag2 %></p>
                        <p><%= prices[i].tag3 %></p>
                        <p><%= prices[i].tag4 %></p>
                        <p><%= prices[i].tag5 %></p>
                </div>
                <% if(isSpider)  { %>
                    <span>
                        <form action="/delete-price?id=<%= prices[i].id %>" method="post">
                            <button class="material-symbols-outlined">delete</button>
                        </form>
                    </span>
                <% } %>
            </div>
        <% } %>
    </div>

    <footer>
        <div>
            <p>&copy Pr1v4cy</p>
        </div>
        <div>
            <ul>
                <li><p>OoSpideroO#5203</p></li>
            </ul>
        </div>
    </footer>
    <script>
        var btn = document.querySelector('.material-symbols-outlined')
        var text = document.querySelector('.text');
        if(btn != null) {
            text.classList.add('hasButton');
        }
    </script>
</body>
</html>